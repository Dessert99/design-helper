# design-helper

[![last commit](https://img.shields.io/github/last-commit/Dessert99/design-helper)](https://github.com/Dessert99/design-helper/commits/main)
[![release](https://img.shields.io/github/v/release/Dessert99/design-helper)](https://github.com/Dessert99/design-helper/releases)

[English](README.md) | [한국어](README.ko.md)

> CSS 값·레이아웃·모션을 글이 아니라 눈으로 정하는 Claude Code 스킬

<!-- 표본 시트 스크린샷 자리 -->

## 왜 필요한가

채팅창에서 "패딩은 12px 이 나을까요, 16px 이 나을까요?" 라고 물으면 답할 수가 없습니다.
숫자만 보고 모양을 떠올릴 수는 없으니까요. 설명을 아무리 잘 써도 결국 코드에 넣고
새로고침해 본 뒤 다시 고치는 일을 반복하게 됩니다.

design-helper 는 이 질문을 말로 하지 않습니다. 후보를 전부 그려서 브라우저에 나란히
띄우고, 사용자는 보고 고르기만 합니다.

- ex) 이 컴포넌트의 테두리 변화를 빠르게 보고 싶다.
- ex) 이 페이지의 기본 레이아웃을 정하고 싶다.

## 하는 일

한 사이클은 이렇게 흘러갑니다.

```
나   카드 그림자 블러를 5가지로 보여줘

     → 프로젝트의 스타일 체계와 현재 카드 값을 읽음
     → 블러만 4 · 8 · 12 · 16 · 24px 로 바꾼 카드 A–E 를 그려 브라우저에 띄움
       (색·농도·위치 등 나머지는 전부 고정)
     → 각 표본 옆에 값과 비용 표시: 토큰 그대로 · --shadow-md 고침 (23곳) · 새 토큰

나   C가 나은데 조금 더 진하게

     → C 를 기준으로 농도 사다리를 아래에 이어 그림 (F · G · H …)

나   G로 갈게

     → 영향 범위 확인 → 실제 카드 코드에 반영 → 계산된 스타일·스크린샷으로 검증 → 보고

나   끝났어

     → 서버를 끄고 임시 파일을 지움
```

규칙은 **주문 하나에 축 하나, 결정 하나**입니다. 한 번에 한 속성만 움직이고 나머지는
고정하기 때문에, 보이는 차이가 곧 그 속성의 차이입니다. 앞에서 고른 값은 다음 비교의
바탕이 됩니다.

- **값을 복사할 필요가 없습니다.** 채팅으로 글자만 말하면 코드 반영까지 스킬이 합니다.
- **새로고침할 필요가 없습니다.** 시트가 바뀌면 스킬이 브라우저 탭을 다시 불러와 앞으로 가져옵니다.
  에이전트에 브라우저 제어 도구가 없으면 그 사실과 시트 주소를 알려 줍니다.
- **프로젝트를 더럽히지 않습니다.** 비교 시트·서버·메모·스크린샷은 전부 프로젝트 밖 임시
  폴더에 있습니다. 프로젝트에 남는 건 고른 값을 반영한 코드뿐이고, 비교 시트는 쓰고 버립니다.
- **프로젝트의 스타일 체계로 그립니다.** Tailwind 와 CSS 변수는 실제 클래스와 토큰으로 그립니다.
  SCSS 변수와 CSS-in-JS 테마는 브라우저가 직접 읽을 수 없어서, 값을 읽어 시트의 CSS 변수로
  옮겨 그립니다. 아무것도 없으면 값을 직접 써서 그립니다.

## 안 하는 것

- **디자인을 창작하지 않습니다.** 자처럼 범위를 펼쳐서 재 줄 뿐이고, 취향은 사용자의 몫입니다.
  사다리 위에서 하나를 추천하지도 않습니다. "B와 C는 이 크기에서 구분이 안 됩니다" 처럼
  눈에 보이는 경계만 짚고, 추천은 직접 요청할 때만 합니다.
- **작업 계획을 대신 세우지 않습니다.** "다음은 색을 보죠" 같은 제안이나 남은 단계 목록이
  없습니다. 다음에 뭘 볼지는 항상 사용자가 정합니다.
- **동작의 정확성은 검증하지 못합니다.** 포커스 트랩, 키보드 조작, 스크린리더는 눈으로 고르는
  문제가 아니라서 이 스킬의 범위 밖입니다.
- **움직임은 사용자의 눈에 맡깁니다.** 스크린샷은 정지 상태만 잡기 때문에, 애니메이션은
  브라우저에서 사용자가 보고 말해 준 것으로 정합니다.

## 요구 사항

- [Claude Code](https://claude.com/claude-code) 또는 Codex
- 브라우저
- `python3`: 시트를 띄우고 자동으로 다시 불러오는 로컬 서버에 씁니다. 없으면 `file://` 로
  열립니다. 이때는 자동 새로고침이 안 되고, 두 분 동안 요청이 없을 때 임시 폴더를 지우는
  자동 정리도 없습니다.
- Playwright (선택): 계산된 스타일과 스크린샷 검증에 씁니다. 없으면 이 검증 단계를 건너뜁니다.

## 설치

아무 데나 clone 한 뒤, 쓰는 에이전트의 스킬 디렉토리에 심링크를 겁니다. 심링크는
복사본이 아니라 원본을 가리키는 포인터라서, clone 을 고치면 바로 반영되고 따로
동기화할 사본이 생기지 않습니다.

```sh
git clone https://github.com/Dessert99/design-helper.git ~/skills/design-helper

ln -sfn ~/skills/design-helper ~/.claude/skills/design-helper   # Claude Code
ln -sfn ~/skills/design-helper ~/.codex/skills/design-helper    # Codex
```

두 에이전트가 읽는 방식은 같습니다. `SKILL.md` 의 `description` 은 세션이 시작할 때
읽고, 본문과 `references/` 는 스킬이 실제로 동작할 때 읽습니다. 그래서 본문 수정은
곧바로 적용되고, `description` 을 고쳤을 때만 세션을 새로 시작하면 됩니다.

지우려면 심링크만 지우면 됩니다 — `rm ~/.claude/skills/design-helper`. clone 은
그대로 남습니다.

## 사용법

UI 를 만들거나 고치는 중에 평소처럼 말하면 됩니다. 이런 말에 스킬이 동작합니다.

- `버튼 radius 5가지 버전으로 보여줘`
- `A 랑 B 중에 뭐가 나아?`
- `카드 여백 비교해줘`
- `이 헤더 좀 어색한데 어떻게 할까` — 무엇을 바꿀지 정해지지 않았으면 한 번에 하나씩 물어봅니다
- `대시보드 레이아웃 잡아줘` — 아무 디자인이 없으면 방향이 크게 다른 조합부터 펼칩니다.
  여기서 고른 조합은 코드에 반영하지 않고 출발점으로만 기록합니다. 그 위에서 속성을 하나씩 정합니다

브라우저에 뜬 표본에는 A · B · C 글자가 붙어 있습니다. 대답은 채팅으로 합니다.

| 이렇게 말하면 | 이렇게 됩니다 |
|---|---|
| `C로 갈게` · `난 B가 좋아` | 선택. 바로 코드에 반영하고 검증합니다 (빈 화면의 첫 조합은 출발점으로만 기록) |
| `C가 나은데 더 진하게` | 추가 비교. 아래에 사다리를 이어 그립니다 |
| `좋네` | 어느 표본인지 되묻습니다 |
| `끝났어` · `됐어` | 서버를 끄고 임시 파일을 정리합니다 |

상황별 예시 대화는 [사용 흐름 모음](docs/flows.ko.md)에 있습니다. 불만은 있는데 뭘 바꿀지
모를 때, 상태(hover·focus) 비교, 애니메이션 비교, 이미 확정한 값 바꾸기 등 11가지 상황을 다룹니다.

## 비슷한 스킬과의 차이

2026년 9월에 각 저장소의 커밋을 기준으로 조사했습니다. 다른 스킬은 대부분 **무엇을 만들지**를
돕고, design-helper 는 **이미 정한 것의 값을 얼마로 할지**를 돕습니다. 그래서 경쟁 관계보다는
함께 쓰는 관계에 가깝습니다.

**[frontend-design](https://github.com/anthropics/skills/tree/33375500bcea98d610eb30ce10ac4e59b89c390d/skills/frontend-design)** (Anthropic 공식)
- 하는 일: 에이전트에게 디자인 리드의 관점을 주고, 뻔한 AI 스타일을 피해 과감한 미감을 고르게 합니다.
- 차이: 후보를 그려 비교하는 절차가 없고, 에이전트가 취향을 정합니다. design-helper 는 취향을 정하지 않고 사용자가 눈으로 고르게 합니다.
- 이럴 때 저쪽: 빈 화면에서 완성도 있는 첫 디자인을 한 번에 얻고 싶을 때. design-helper 는 방향을 창작해 주지 않습니다.

**[impeccable](https://github.com/pbakaus/impeccable/tree/9d715cc4f5564a990ca8345abfdd5df6dc9b41c8)** (pbakaus)
- 하는 일: 디자인 어휘와 안티패턴 검출 규칙, `audit` · `polish` · `typeset` · `layout` 같은 명령을 제공합니다. live 모드에서는 실행 중인 앱의 요소를 골라 변형을 브라우저에서 넘겨 보고, 받아들인 변형을 소스에 씁니다. 기존 CSS 토큰과 계산된 스타일을 읽고, 변형마다 조절 슬라이더를 붙일 수도 있습니다.
- 차이: 가장 가까운 스킬입니다. impeccable 의 변형은 요소를 통째로 다시 만든 제안이라 여러 속성이 함께 바뀌고, 한 번에 하나씩 넘겨 봅니다. design-helper 는 속성 하나만 움직인 사다리를 한 화면에 나란히 놓고, 표본마다 토큰 영향 범위를 표시합니다.
- 이럴 때 저쪽: 실제 앱 화면 위에서 바로 고르고 싶을 때, 디자인 감사와 다듬기를 명령 하나로 돌리고 싶을 때.

**[visual companion](https://github.com/obra/superpowers/blob/8ca22dba9a94f28898bbce59f2537ff4d87c747d/skills/brainstorming/visual-companion.md)** (obra/superpowers, brainstorming 스킬의 일부)
- 하는 일: 구현 전 브레인스토밍 단계에서 목업·다이어그램·나란히 비교를 브라우저에 띄우고, 사용자가 클릭으로 고릅니다. 간격이나 시각적 위계 같은 다듬기 질문에도 씁니다.
- 차이: 한 속성만 움직이는 사다리, 선택 후 코드 반영과 검증을 절차로 정해 두지는 않습니다. design-helper 는 이 세 가지가 기본 절차입니다.
- 이럴 때 저쪽: 아키텍처 다이어그램처럼 UI 밖의 시각화가 필요할 때, 구현 전 기획 단계에서 방향을 잡을 때.

**[design-shotgun](https://github.com/garrytan/gstack/blob/2a113ae7e623f590095bcaaa0cc581c9a10a6632/design-shotgun/SKILL.md.tmpl)** (garrytan/gstack)
- 하는 일: 이미지 생성으로 서로 다른 디자인 시안 3~8장을 만들어 비교 보드에 띄우고, 평가와 메모를 받아 반복합니다. 승인된 취향을 기억합니다.
- 차이: 결과물이 코드가 아니라 이미지(PNG)이고, 코드 반영은 다른 스킬의 몫입니다. 이미지 생성 도구가 필요합니다. design-helper 는 별도 생성 도구 없이 CSS 로 그립니다.
- 이럴 때 저쪽: "뭘 원하는지 아직 모르겠다"는 단계에서 넓게 발산하고 싶을 때.

**design-helper 가 강조하는 작업 방식**: 한 번에 한 속성만 움직이는 사다리를 나란히 비교하고, 표본마다 영향 범위를 표시하고, 사다리 위에서 추천하지 않고, 선택하면 반영과 검증까지 이어 가고, 작업 파일은 프로젝트 밖에만 둡니다.

**design-helper 의 약점**: 디자인 방향을 제안하지 않고, 안티패턴 자동 검출이 없고, 실제 앱 화면이 아니라 별도 시트에서 비교하고, 기본으로 한국어로만 답합니다.

## 언어 설정

지시문은 영어로 쓰여 있지만, 채팅 답변과 비교 시트에 표시되는 문자열은 전부 한국어입니다.
코드·클래스 이름·파일 이름·커밋 메시지는 영어로 둡니다.

다른 언어로 바꾸려면 `SKILL.md` 의 `## Language` 섹션을 고치면 됩니다. 지시문 곳곳에
인용된 한국어 고정 문구(`토큰 그대로`, `끝났어` 등)도 함께 바꿔야 합니다.

## 문서

- [`SKILL.md`](SKILL.md) — 스킬 본체. 작업 단위, 표본 그리는 규칙, 응답 읽기, 검증, 종료
- [`references/`](references/) — 스킬이 필요할 때 읽는 세부 지침
  - [`clarification.md`](references/clarification.md) — 모호한 요청에서 질문하는 법
  - [`sweeping.md`](references/sweeping.md) — 후보 스케일, 배경·크기 맥락, 빈 화면에서 시작하기, 와이어프레임
  - [`stylesystems.md`](references/stylesystems.md) — 스타일 체계 감지와 체계별 그리는 법
  - [`controls.md`](references/controls.md) — 선택적 슬라이더와 시트 엔진
  - [`motion.md`](references/motion.md) — 애니메이션 비교
  - [`liveview.md`](references/liveview.md) — 임시 작업 폴더, 로컬 서버, 브라우저 탭 관리, 정리
- [`docs/flows.ko.md`](docs/flows.ko.md) — 상황별 사용 흐름 (한국어)

## 라이선스

[MIT](LICENSE)
